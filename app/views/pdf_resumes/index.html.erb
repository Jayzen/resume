<% provide(:title, '上传简历') %>
<div class="row">
  <%= render 'shared/basic_bar' %>
  <% if current_user.pdf_resume.id? %>
    <%= render 'pdf_resumes' %>
  <% else %>
    <%= render 'pdf_resume_new' %>
  <% end %>
</div>
