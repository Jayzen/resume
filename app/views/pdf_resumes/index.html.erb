<% provide(:title, t('pdf_resume_uploaded')) %>
<div class="row">
  <%= render 'shared/resume_bar' %>
  <% if current_user.pdf_resumes %>
    <%= render 'pdf_resumes' %>
  <% else %>
    <%= render 'pdf_resume_new' %>
  <% end %>
</div>
